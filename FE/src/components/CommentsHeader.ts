const CommentsHeader = () => {
  const template = `
    <nav class="navbar navbar-dark bg-secondary">
      <div class="container-fluid justify-content-center">
        <span class="navbar-brand mb-0 h1">댓글</span>
      </div>
    </nav>
  `;

  return template;
};

export default CommentsHeader;
